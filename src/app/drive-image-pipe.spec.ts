import { DriveImagePipe } from './drive-image-pipe';

describe('DriveImagePipe', () => {
  it('create an instance', () => {
    const pipe = new DriveImagePipe();
    expect(pipe).toBeTruthy();
  });
});
